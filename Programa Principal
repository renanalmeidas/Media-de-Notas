def M_NOTAS():
    q = int(input('Quantas notas vamos somar? '))
    notas = q * [0]
    media = 0
    for cont in range(q):
        print('nota', cont + 1, ': ')
        notas[cont] = float(input())
        media += notas[cont]
    print(media/q)
    return
M_NOTAS()
